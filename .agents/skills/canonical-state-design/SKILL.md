# Canonical State Design Skill

Define one typed JSON-serializable model.

Ensure it can represent:

- template ID
- stable element IDs
- element types
- editable properties
- base values
- viewport overrides
- revision/version
- history

Define which state is:

- canonical
- derived
- temporary UI state

Ensure editing surfaces cannot diverge.
