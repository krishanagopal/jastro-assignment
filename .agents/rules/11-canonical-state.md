# Canonical State Rules

The template model is the durable source of truth.

Do not create competing authoritative state for:

- canvas
- code
- AI
- history

Temporary UI state may exist separately.

Committed template data must remain canonical and typed.

Canvas and code must remain synchronized through the same
canonical model.
