# AI Scope Safety Rules

Do not connect a real AI model.

Use a deterministic scenario engine.

AI proposals may affect only:

- selected IDs
- allowed fields
- chosen viewport scope

AI output must never automatically overwrite state.

Every proposal must be validated.

Every element must support independent acceptance or rejection.
